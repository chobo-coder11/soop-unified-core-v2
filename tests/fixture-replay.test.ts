import test from'node:test';import assert from'node:assert/strict';import{buildPacket}from'../src/protocol/packet.js';import{F}from'../src/protocol/constants.js';import{encodeFixturePackets,readFixtureBuffer,replayFixtureBuffer}from'../src/protocol/fixture-replay.js';
test('length-prefixed raw fixture roundtrip preserves packets and decodes',()=>{const packets=[buildPacket(5,`${F}안녕${F}user${F.repeat(3)}nick${F}`),buildPacket(104,`${F}notice${F}`)];const fixture=encodeFixturePackets(packets);assert.deepEqual(readFixtureBuffer(fixture),packets);const events=replayFixtureBuffer('streamer',fixture);assert.equal(events.length,2);assert.equal(events[0].code,5);assert.equal(events[1].code,104)});
test('fixture reader rejects truncated or absurd packet lengths',()=>{assert.throws(()=>readFixtureBuffer(Buffer.from([0,0,0,10,1,2])),/truncated/);const b=Buffer.alloc(4);b.writeUInt32BE(0xffffffff);assert.throws(()=>readFixtureBuffer(b),/invalid fixture packet length/)});
