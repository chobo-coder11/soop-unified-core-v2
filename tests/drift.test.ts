import test from'node:test';import assert from'node:assert/strict';import{ProtocolDriftDetector}from'../src/core/protocol-drift.js';
const ev=(streamerId:string,code:number,unknown:boolean)=>({id:Math.random().toString(),streamerId,code,type:unknown?'UNKNOWN':'CHAT_MESSAGE',description:'',category:unknown?'unknown':'chat',supportLevel:'stable',source:'native',receivedAt:new Date().toISOString(),payload:{}} as any);
test('drift detector raises incident on multi-stream unknown burst',()=>{const d=new ProtocolDriftDetector({windowMs:60000,minUnknown:3,minMalformed:3,minStreams:2,unknownRatio:.3,alertCooldownMs:0});d.observeEvent(ev('a',5,false));d.observeEvent(ev('a',130,true));d.observeEvent(ev('b',130,true));const incident=d.observeEvent(ev('b',131,true));assert.ok(incident);assert.equal(incident!.reason,'unknown-rate');assert.equal(incident!.affectedStreams,2)});
