SOOP Unified Core v2.6.0 Accuracy Hardened build manifest
Generated: 2026-08-31 Asia/Seoul

VALIDATION GATES
- Repository integrity: REQUIRED
- TypeScript Node 22/24 build: REQUIRED
- Source regression tests: REQUIRED
- Compiled regression tests: REQUIRED
- Java 25 sidecar compile: REQUIRED
- Node and Java Docker image build: REQUIRED
- Real SOOP smoke / soak: separate network validation; not implied by CI

V2.6 MISSION ACCURACY
- opcode 121 treated as mission envelope, not challenge-only
- delimiter-free and field-delimited mission JSON parsing
- CHALLENGE_GIFT -> CHALLENGE_MISSION_GIFTED / challenge_mission
- opcode-121 GIFT -> BATTLE_MISSION_GIFTED / battle_mission
- donor/count snake_case + camelCase compatibility
- JSON Unicode nickname decoding
- chno/key/title/relay/status/uuid + parsed mission data preservation
- unknown subtype remains notification; malformed payload becomes raw-only
- opcode 125 settlement payload preservation without unverified subtype guessing
- legacy MISSION WebSocket subscription compatibility retained
- dedicated mission and filter regression tests required by repository integrity

V2.5 ACCURACY CORE RETAINED
- strict SOOP wire normalization and fallback canonicalization
- impossible snapshot quarantine
- upstream-family evidence consensus
- per-field agreement/confidence/temporal skew/importance
- BNO broadcast generation tracking
- live BNO ↔ station broadNo mismatch revalidation
- socket BNO ↔ current live BNO binding diagnostics
- VIEWPRESET-aware JOIN metadata
- legacy-v1-ticket + browser-v2-null watchdog fallback profiles
- opcode 127 shape-aware ambiguous decoding with RAW preservation
- Studio broadcast identity and field-evidence diagnostics

V2.4 P0 HARDENING RETAINED
- provider hard deadlines / partial state / explicit stale freshness
- signal-isolated adaptive reliability
- WS gap + replay/resume + serialized subscriptions
- drift/dedup/raw hot-path optimization
- connection timeline / possible gap history / runtime metrics
- /livez /readyz and operator Studio

IMPORTANT
Evidence confidence is an operational heuristic score, not a calibrated probability.
Unknown protocol semantics are preserved rather than guessed.
The release tree intentionally excludes node_modules, .env and credentials.
