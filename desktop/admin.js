let state={boxes:[],pending:[],streamerId:''};const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function toast(s){$('toast').textContent=s;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2100)}async function api(path,opts){const r=await fetch('/v1/roulette'+path,{headers:{'content-type':'application/json'},...opts}),j=await r.json();if(!r.ok)throw Error(j.error||'처리하지 못했습니다.');return j}
function render(){const list=state.boxes.filter(b=>b.status!=='archived');$('boxes').innerHTML=list.length?list.map(b=>{const p=Math.min(100,b.collected/b.goal*100);return `<article class="box ${b.status==='completed'?'done':''}"><div class="box-top"><div><h3>${esc(b.title)}</h3><small>${b.status==='completed'?'🎉 추첨 완료':'후원 메시지에 입력'}</small></div><span class="tag">#${esc(b.code)}</span></div><div class="track"><div class="fill" style="width:${p}%"></div></div><div class="row"><b>${b.collected.toLocaleString()}개</b><span>${b.goal.toLocaleString()}개 · ${p.toFixed(1)}%</span></div><div class="shares">${b.winner?`<div class="row"><b>🏆 ${esc(b.winner.nickname)}</b><b>당첨</b></div>`:b.shares.slice().sort((a,c)=>c.amount-a.amount).slice(0,5).map(s=>`<div class="row"><span>${esc(s.nickname)}</span><b>${s.amount.toLocaleString()}개 · ${(s.amount/b.goal*100).toFixed(1)}%</b></div>`).join('')}</div><button class="danger" onclick="archiveBox('${b.id}')">${b.status==='completed'?'목록에서 정리':'상자 종료'}</button></article>`}).join(''):'<div class="empty"><div style="font-size:40px">📦</div><b>아직 열린 상자가 없습니다</b><br>오른쪽에서 첫 상자를 열어보세요.</div>';$('pending').innerHTML=state.pending.length?state.pending.map(p=>`<div class="row"><span>${esc(p.nickname)}</span><b>${p.amount.toLocaleString()}개</b></div>`).join(''):'<div class="hint" style="padding:12px 0">보관 중인 별풍선이 없습니다.</div>';$('streamer').value=state.streamerId||'';$('connection').textContent=state.streamerId?'실시간 후원 연결됨':'SOOP 연결 준비';$('streamLabel').textContent=state.streamerId?state.streamerId+' 방송을 듣는 중':'아이디를 입력하세요'}
async function createBox(){try{await api('/boxes',{method:'POST',body:JSON.stringify({title:$('titleInput').value,code:$('code').value,goal:Number($('goal').value)})});$('titleInput').value=$('code').value=$('goal').value='';toast('새 상자를 열었습니다.')}catch(e){toast(e.message)}}async function archiveBox(id){if(confirm('상자를 종료할까요? 모인 지분은 보관함으로 돌아갑니다.'))await api('/boxes/'+id,{method:'DELETE'})}async function connectStream(){try{await api('/settings',{method:'POST',body:JSON.stringify({streamerId:$('streamer').value})});toast('방송과 연결했습니다.')}catch(e){toast(e.message)}}async function testDonation(){try{await api('/test-donation',{method:'POST',body:JSON.stringify({nickname:$('testNick').value,amount:Number($('testAmount').value),message:$('testMessage').value})});toast('가상 후원을 보냈습니다.')}catch(e){toast(e.message)}}function copyOverlay(){navigator.clipboard.writeText($('overlayUrl').value);toast('방송 화면 주소를 복사했습니다.')}$('overlayUrl').value=location.origin+'/roulette/overlay';new EventSource('/v1/roulette/events').onmessage=e=>{state=JSON.parse(e.data).state;render()};api('/state').then(x=>{state=x.state;render()});
