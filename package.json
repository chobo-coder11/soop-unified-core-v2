{
  "name": "soop-unified-core",
  "version": "2.4.0",
  "private": true,
  "description": "Production-oriented unofficial SOOP unified core with provider deadlines, partial-state recovery, explicit freshness, field consensus, replayable realtime delivery, optimized hot paths, runtime diagnostics and operator Studio.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "verify:repo": "node scripts/verify-repository.mjs",
    "build": "npm run verify:repo && tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "test": "npm run verify:repo && tsx --test tests/*.test.ts",
    "smoke": "tsx scripts/smoke.ts",
    "test:compiled": "node --test dist/tests/*.test.js",
    "soak": "tsx scripts/soak.ts"
  },
  "dependencies": {
    "ws": "8.18.0"
  },
  "optionalDependencies": {
    "soop-extension": "1.3.3",
    "soop.js": "1.1.1"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "@types/ws": "8.5.13",
    "tsx": "4.19.2",
    "typescript": "5.7.2"
  }
}
