export function httpStatusForError(message:string){const m=message.toLowerCase();if(m==='invalid_json'||m.includes('invalid credentials')||m.includes('invalid chat message')||m.includes('invalid whisper message')||m.includes('invalid target id'))return 400;if(m==='request_body_too_large')return 413;if(m.includes('invalid auth session')||m.includes('auth session expired')||m.includes('authenticated session required'))return 401;if(m.includes('write api disabled'))return 403;if(m.includes('stream offline'))return 503;return 502}
