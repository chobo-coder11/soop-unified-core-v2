import type{ChannelSnapshot,LiveSnapshot,ProviderObservation}from'../types.js';import{SoopHttpClient}from'../http/soop-http.js';import{CircuitBreaker}from'../core/circuit-breaker.js';import{PROVIDER_PROVENANCE}from'../core/provider-provenance.js';
export class NativeProvider{readonly breaker=new CircuitBreaker('native',true);private http:SoopHttpClient;constructor(timeoutMs:number){this.http=new SoopHttpClient(timeoutMs)}private async observe<T>(fn:()=>Promise<T>):Promise<ProviderObservation<T>>{const s=Date.now();try{const value=await this.breaker.run(fn);return{provider:'native',ok:true,latencyMs:Date.now()-s,observedAt:new Date().toISOString(),value,provenance:PROVIDER_PROVENANCE.native}}catch(e){return{provider:'native',ok:false,latencyMs:Date.now()-s,observedAt:new Date().toISOString(),error:e instanceof Error?e.message:String(e),provenance:PROVIDER_PROVENANCE.native}}}live(id:string){return this.observe<LiveSnapshot>(()=>this.http.liveDetail(id))}channel(id:string){return this.observe<ChannelSnapshot>(()=>this.http.station(id))}directHttp(){return this.http}}
